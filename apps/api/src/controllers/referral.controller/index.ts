import { NextFunction, Request, Response } from 'express';

export async function findReferralNumber(
  req: Request,
  res: Response,
  next: NextFunction,
) {
    try {
      const
  } catch (error) {
    next(error);
  }
}
